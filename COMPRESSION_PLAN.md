# Plan: OpenProxy Context Compression — Phase 1 (high ROI)

Decision (user): Option A (flat toggles), CCR v1 marker-only (no retrieve), RTK default standard + caveman default lite.

Order: session-dedup → lite → ccr-v1 → codex-responses → rtk-config → settings/UI/stats.
Status: PENDING
